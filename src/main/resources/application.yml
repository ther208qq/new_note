
spring:
#应用名称
  application:
    name: springboot-web

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://mysql:3306/notes_database?serverTimezone=Asia/Shanghai&characterEncoding=utf-8
    username: root
    password: 1234

mybatis:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  mapper-locations: classpath:**mapper/*.xml