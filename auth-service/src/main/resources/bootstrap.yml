spring:
  application:
    name: auth-service
  profiles:
    active: dev
    include: redis
  cloud:
    nacos:
      server-addr: 192.168.88.100:8848
      config:
        file-extension: yaml
        shared-configs:
          - data-id: shared-jdbc.yml
  data:
    redis:
      repositories:
        enabled: false